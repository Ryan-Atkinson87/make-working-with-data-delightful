

import numpy as np
import pandas as pd

d = {'x': [1, 2, 3], 'y': np.array([2, 4, 8]), 'z': 100}

# pd.DataFrame(d)
# use to show the dataframe table

# pd.DataFrame(d, index=[100, 200, 300], columns=['z', 'y', 'x'])
# use to control the order of the columns "columns"and
# change the row labels with "index"

print("completed")

